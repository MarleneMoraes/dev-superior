package com.devsuperior.javacompleto.aula133;

// public class SavingsAccountPlus extends SavingsAccount { // The type SavingsAccountPlus cannot subclass the final class SavingsAccount
public class SavingsAccountPlus {
	/* 
	 * @Override
	 * public final void withDraw(double amount) { // The method withDraw(double) of type SavingsAccountPlus must override or implement a supertype method
	 * 		this.balance -= amount + 2.0;
	 * }
	 * */
	
}
